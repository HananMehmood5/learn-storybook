import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { select, text, withKnobs } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { TASK_STATES } from "../../types"

import Task from '.';

<Meta title="components/Task" component={Task}/>

# Task

Basic component

## **Props**
<Props of={Task} />

## **Example Usage**

<br />
<br />

### **_Default_**

<Preview>
    <Story name="default">
        <Task 
            id= "1"
            title= "Test Task 2"
            state= {TASK_STATES.DEFAULT}
            onArchiveTask={() => {}}
            onPinTask={() => {}}
        />
    </Story>
</Preview>

### **_Default With Knobs_**

<Preview>
    <Story name="default With Knobs">
        <Task 
            id= "1"
            title= {text("Title", "Test Task 2")}
            state= {select(
                "State",
                {
                    TASK_ARCHIVED: TASK_STATES.TASK_ARCHIVED,
                    TASK_PINNED: TASK_STATES.TASK_PINNED,
                    DEFAULT : TASK_STATES.DEFAULT,
                },
                TASK_STATES.DEFAULT,
            )}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>

### **_Archived_**

<Preview>
    <Story name="Archived">
        <Task 
            id= "1"
            title= {text("Title", "Test Task 2")}
            state= {select(
                "State",
                {
                    TASK_ARCHIVED: TASK_STATES.TASK_ARCHIVED,
                    TASK_PINNED: TASK_STATES.TASK_PINNED,
                    DEFAULT :TASK_STATES.DEFAULT,
                },
                TASK_STATES.TASK_ARCHIVED,                
            )}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>

### **_Pinned_**

<Preview>
    <Story name="Pinned">
        <Task 
            id= "1"
            title= {text("Title", "Test Task 2")}
            state= {select(
                "State",
                {
                    TASK_ARCHIVED: TASK_STATES.TASK_ARCHIVED,
                    TASK_PINNED: TASK_STATES.TASK_PINNED,
                    DEFAULT :TASK_STATES.DEFAULT,
                },
                TASK_STATES.TASK_PINNED,                
            )}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>