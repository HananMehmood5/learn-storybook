import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { select, text, withKnobs } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { TASK_STATES } from "../../types"

import Task from '.';

<Meta title="components/Task" component={Task} decorators={[withKnobs]}/>

# Props for Task
<Props of={Task} />

# Stories for Task

Let's define a story for our `Task`:

<Preview>
    <Story name="default">
        <Task 
            id= "1"
            title= "Test Task 2"
            state= {TASK_STATES.DEFAULT}
            onArchiveTask={() => {}}
            onPinTask={() => {}}
        />
    </Story>
</Preview>

## Default with "Preview" and with "withKnobs"

<Preview>
    <Story name="default With Knobs">
        <Task 
            id= "1"
            title= {text("Title", "Test Task 2")}
            state= {select(
                "State",
                {
                    TASK_ARCHIVED: TASK_STATES.TASK_ARCHIVED,
                    TASK_PINNED: TASK_STATES.TASK_PINNED,
                    DEFAULT : TASK_STATES.DEFAULT,
                },
                TASK_STATES.DEFAULT,
            )}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>

## Archived:

<Preview>
    <Story name="Archived">
        <Task 
            id= "1"
            title= {text("Title", "Test Task 2")}
            state= {select(
                "State",
                {
                    TASK_ARCHIVED: TASK_STATES.TASK_ARCHIVED,
                    TASK_PINNED: TASK_STATES.TASK_PINNED,
                    DEFAULT :TASK_STATES.DEFAULT,
                },
                TASK_STATES.TASK_ARCHIVED,                
            )}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>

## Pinned:

<Preview>
    <Story name="Pinned">
        <Task 
            id= "1"
            title= {text("Title", "Test Task 2")}
            state= {select(
                "State",
                {
                    TASK_ARCHIVED: TASK_STATES.TASK_ARCHIVED,
                    TASK_PINNED: TASK_STATES.TASK_PINNED,
                    DEFAULT :TASK_STATES.DEFAULT,
                },
                TASK_STATES.TASK_PINNED,                
            )}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>