import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { select, text, withKnobs } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { TASK_STATES } from "../../types"

import Task from '.';

<Meta title="components/Task" component={Task}/>

# Task

Basic component

## **Props**
<Props of={Task} />

# Stories for Task

Let's define a story for our `Task` component with `Preview`:

## **Example Usage**

<br />

### **_Default_**

<Preview>
    <Story name="default">
        <Task 
            id= "1"
            title= {text("Title", "Test Task 2")}
            state= {select(
                "State",
                [
                    TASK_STATES.DEFAULT,
                    TASK_STATES.TASK_ARCHIVED,
                    TASK_STATES.TASK_PINNED
                ]
            )}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>