import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { boolean, withKnobs } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { Tasks, PinnedTasks  } from "../../mocks/task"

import TaskList from '.';

<Meta title="components/TaskList" component={TaskList} decorators={[withKnobs]}/>

# Props for Task
<Props of={TaskList} />

# Stories for TaskList

Let's define a story for our `TaskList`:

<Preview>
    <Story name="default">
        <TaskList 
            loading={boolean("loadinhg", false)}
            tasks= {Tasks}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>

<Preview>
    <Story name="Pinned tasks">
        <TaskList 
            loading={boolean("loadinhg", false)}
            tasks= {PinnedTasks}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>


<Preview>
    <Story name="loading">
        <TaskList 
            loading={boolean("loadinhg", true)}
            tasks= {[]}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>

<Preview>
    <Story name="empty">
        <TaskList 
            loading={boolean("loadinhg", false)}
            tasks= {[]}
            onArchiveTask={action('archive Task')}
            onPinTask={action('Pin Task')}
        />
    </Story>
</Preview>

